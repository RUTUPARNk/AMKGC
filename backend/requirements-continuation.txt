# Continuation Service Dependencies
psycopg[binary]>=3.0

# For token counting (optional, but recommended)
# tiktoken>=0.5.0  # For OpenAI token counting

# For JSON operations (built-in, but specifying for completeness)
# No additional dependencies needed for basic JSON
